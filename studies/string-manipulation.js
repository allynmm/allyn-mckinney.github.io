/**
 * STRING MANIPULATION:
 * 
 * 0. With operators
 * 
 * 1. With string methods
 */

// 1. Addition Operator
// The addition operator is used to join or concatenate strings
    var greeting = "Hello ";
    var user = 'Allyn';
    var punctuation = "!";

    var message = greeting + user + punctuation;

// 2. Addition Assignment Operator
// The Addition Assignment Operator appends whatever is to the right of the operand to the string on the left of the operand
    var output = 'hello ';
    output += 'world';

// 3. .slice() method
// Returns a portion of the selected string depending on the arguments passed

// 4. .toLowerCase() method

// 5. .toUpperCase() method

// 6. .concat() method

// 7. .split() method